import { AlertTriangle, ArrowRight, BadgeDollarSign, Bell, Boxes, CircleDollarSign, ClipboardList, ReceiptText, ShoppingCart, TrendingUp, Wallet } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { BarChart, DonutChart } from "@/components/crm/Charts";
import { CrmPageHeader, StatusBadge } from "@/components/crm/CrmUI";
import { MetricCard } from "@/components/crm/MetricCard";
import { financeService } from "@/services/FinanceService";
import { useAppStore } from "@/stores/useAppStore";
import { formatDateTime } from "@/utils/date";
import { formatMoney } from "@/utils/money";
import { isOrderFinanciallyRecognized } from "@/utils/orders";

type DashboardPeriod = "today" | "week" | "month" | "quarter";
const PERIOD_LABELS: Record<DashboardPeriod, string> = { today: "сегодня", week: "за 7 дней", month: "за 30 дней", quarter: "за квартал" };
const periodStart = (period: DashboardPeriod) => { const date = new Date(); if (period === "today") { date.setHours(0,0,0,0); return date; } const days = period === "week" ? 6 : period === "month" ? 29 : 89; date.setDate(date.getDate()-days); date.setHours(0,0,0,0); return date; };

export function DashboardSection({ role }: { role: "admin" | "manager" }) {
  const [period, setPeriod] = useState<DashboardPeriod>("month");
  const session = useAppStore((state) => state.session)!; const allLeads = useAppStore((state) => state.leads); const allOrders = useAppStore((state) => state.orders); const products = useAppStore((state) => state.products); const managers = useAppStore((state) => state.managers); const notifications = useAppStore((state) => state.notifications); const expenses = useAppStore((state) => state.expenses); const returns = useAppStore((state) => state.returns); const managerCommissions = useAppStore((state) => state.managerCommissions);
  const managerId = session.managerProfileId; const start = useMemo(() => periodStart(period), [period]); const startMs=start.getTime(); const inPeriod=(value:string) => { const time=new Date(value).getTime(); return Number.isFinite(time) && time>=startMs; };
  const scopedLeads = role === "admin" ? allLeads : allLeads.filter((lead) => lead.managerId===managerId); const scopedOrders = role === "admin" ? allOrders : allOrders.filter((order) => order.managerId===managerId); const leads=scopedLeads.filter((lead)=>inPeriod(lead.createdAt)); const orders=scopedOrders.filter((order)=>inPeriod(order.createdAt)); const recognizedOrders=orders.filter(isOrderFinanciallyRecognized); const recognizedIds=new Set(recognizedOrders.map((order)=>order.id));
  const currentSummary=financeService.summary(); const revenue=recognizedOrders.reduce((sum,order)=>sum+order.total,0); const costOfGoods=recognizedOrders.reduce((sum,order)=>sum+order.items.reduce((itemSum,item)=>itemSum+item.purchasePrice*item.quantity,0),0); const periodExpenses=expenses.filter((item)=>item.category!=="traffic_fee" && inPeriod(item.date)).reduce((sum,item)=>sum+item.amount,0); const periodReturns=returns.filter((item)=>inPeriod(item.createdAt) && recognizedIds.has(item.orderId)).reduce((sum,item)=>sum+item.amount,0); const periodCommissions=managerCommissions.filter((item)=>item.status!=="cancelled" && recognizedIds.has(item.orderId)).reduce((sum,item)=>sum+item.amount,0); const profit=revenue-costOfGoods-periodCommissions-periodExpenses-periodReturns;
  const commission=role === "manager" ? managerCommissions.filter((item)=>item.managerId===managerId && item.status!=="cancelled" && inPeriod(item.createdAt)).reduce((sum,item)=>sum+item.amount,0) : 0; const newLeads=leads.filter((lead)=>lead.status==="new").length; const completed=leads.filter((lead)=>lead.status==="completed").length; const conversion=leads.length?Math.round(completed/leads.length*100):0; const lowStock=products.filter((product)=>product.stock-product.reserved<=product.minimumStock);
  const salesData=useMemo(() => { const buckets=Array.from({length:7},(_,index)=>({label:"",value:0})); const end=Date.now(), span=Math.max(1,end-startMs); for (let index=0;index<7;index+=1) { if (period==="today") buckets[index].label=`${String(Math.round(index*24/7)).padStart(2,"0")}:00`; else { const date=new Date(startMs+span*(index/7)); buckets[index].label=date.toLocaleDateString("ru-RU",{day:"2-digit",month:"2-digit"}); } } recognizedOrders.forEach((order)=>{ const ratio=(new Date(order.createdAt).getTime()-startMs)/span; const index=Math.max(0,Math.min(6,Math.floor(ratio*7))); buckets[index].value+=1; }); return buckets; },[recognizedOrders,startMs,period]);
  const topProducts=[...products].sort((a,b)=>b.views-a.views).slice(0,5); const managerStats=useMemo(()=>managers.map((manager)=>({...manager,conversion:manager.leadCount?Math.round(manager.salesCount/manager.leadCount*100):0})).sort((a,b)=>b.salesCount-a.salesCount),[managers]); const rolePath=role==="admin"?"admin":"manager"; const label=PERIOD_LABELS[period];
  return <div className="crm-page dashboard-section"><CrmPageHeader title={role==="admin"?`Добрый день, ${session.name.split(" ")[0]}!`:`Ваш рабочий день, ${session.name.split(" ")[0]}`} text={role==="admin"?`Ключевые показатели магазина ${label}. Период ниже теперь применяется к цифрам и графику.`:`Ваши заявки, продажи и начисления ${label}.`} actions={<select className="crm-period-select" value={period} onChange={(event)=>setPeriod(event.target.value as DashboardPeriod)}><option value="today">Сегодня</option><option value="week">7 дней</option><option value="month">30 дней</option><option value="quarter">Квартал</option></select>}/>{role==="admin"?<div className="metrics-grid"><MetricCard label={`Новые заявки · ${label}`} value={String(newLeads)} helper={`${leads.filter((lead)=>lead.status==="working").length} уже в работе`} icon={ClipboardList} accent="blue"/><MetricCard label={`Продажи · ${label}`} value={formatMoney(revenue)} helper={`${recognizedOrders.length} завершённых продаж`} icon={ShoppingCart} accent="green"/><MetricCard label={`Чистая прибыль · ${label}`} value={formatMoney(profit)} helper="Продажи минус себестоимость, комиссии, расходы и возвраты" icon={TrendingUp} accent="violet"/><MetricCard label={`Расходы · ${label}`} value={formatMoney(periodExpenses)} helper="Без Трафик системы" icon={ReceiptText} accent="orange"/><MetricCard label="На складе сейчас" value={`${products.reduce((sum,product)=>sum+product.stock,0)} шт.`} helper={`${lowStock.length} позиций требуют внимания`} icon={Boxes} accent="cyan"/><MetricCard label="Долги поставщикам сейчас" value={formatMoney(currentSummary.supplierDebt)} helper="Текущий непогашенный остаток" icon={Wallet} accent="red"/></div>:<div className="metrics-grid metrics-grid--manager"><MetricCard label={`Мои новые заявки · ${label}`} value={String(newLeads)} helper={`${leads.length} всего за период`} icon={ClipboardList} accent="blue"/><MetricCard label={`Мои продажи · ${label}`} value={formatMoney(revenue)} helper={`${recognizedOrders.length} завершённых продаж`} icon={ShoppingCart} accent="green"/><MetricCard label={`Конверсия · ${label}`} value={`${conversion}%`} helper={`${completed} завершённых заявок`} icon={TrendingUp} accent="violet"/><MetricCard label={`Мои начисления · ${label}`} value={formatMoney(commission)} helper="Начислено за выбранный период" icon={BadgeDollarSign} accent="orange"/></div>}<div className="dashboard-main-grid"><section className="crm-panel sales-chart-panel"><header><div><span>Продажи</span><h3>Динамика {label}</h3></div><b>{recognizedOrders.length}</b></header><BarChart data={salesData} color="#4f7cff"/><footer><div><span className="legend-dot legend-dot--blue"/>Завершено</div><strong>{recognizedOrders.length} заказов</strong></footer></section><section className="crm-panel conversion-panel"><header><div><span>Воронка</span><h3>Конверсия заявок {label}</h3></div></header><div className="conversion-body"><DonutChart value={completed} total={Math.max(leads.length,1)} label="в продажу"/><dl><div><dt>Всего заявок</dt><dd>{leads.length}</dd></div><div><dt>В работе</dt><dd>{leads.filter((lead)=>["working","consulted","confirmed"].includes(lead.status)).length}</dd></div><div><dt>Завершено</dt><dd>{completed}</dd></div><div><dt>Отказ</dt><dd>{leads.filter((lead)=>lead.status==="refused").length}</dd></div></dl></div><Link to={`/crm/${rolePath}/funnel`}>Открыть воронку <ArrowRight size={16}/></Link></section></div><div className="dashboard-secondary-grid"><section className="crm-panel table-panel"><header><div><span>За выбранный период</span><h3>Новые заявки</h3></div><Link to={`/crm/${rolePath}/leads`}>Все заявки <ArrowRight size={15}/></Link></header><div className="responsive-table"><table><thead><tr><th>Заявка</th><th>Клиент</th><th>Сумма</th><th>Статус</th><th>Дата</th></tr></thead><tbody>{leads.slice(-5).reverse().map((lead)=><tr key={lead.id}><td><strong>{lead.number}</strong></td><td><span>{lead.fullName}</span><small>{lead.phone}</small></td><td>{formatMoney(lead.total)}</td><td><StatusBadge status={lead.status}/></td><td>{formatDateTime(lead.createdAt)}</td></tr>)}</tbody></table></div></section><section className="crm-panel attention-panel"><header><div><span>Контроль</span><h3>Требует внимания</h3></div></header><div className="attention-list"><Link to={`/crm/${rolePath}/${role==="admin"?"inventory":"catalog"}`}><span className="attention-icon attention-icon--orange"><AlertTriangle size={18}/></span><div><strong>Низкий остаток</strong><small>{lowStock.length} товаров достигли минимума</small></div><ArrowRight size={16}/></Link><Link to={`/crm/${rolePath}/leads`}><span className="attention-icon attention-icon--blue"><ClipboardList size={18}/></span><div><strong>Необработанные заявки</strong><small>{newLeads} клиентов {label} ждут ответа</small></div><ArrowRight size={16}/></Link>{role==="admin"&&<Link to="/crm/admin/finance"><span className="attention-icon attention-icon--violet"><CircleDollarSign size={18}/></span><div><strong>Задолженности</strong><small>{formatMoney(currentSummary.supplierDebt+currentSummary.managerDebt)}</small></div><ArrowRight size={16}/></Link>}<Link to={`/crm/${rolePath}/notifications`}><span className="attention-icon attention-icon--red"><Bell size={18}/></span><div><strong>Непрочитанные</strong><small>{notifications.filter((item)=>!item.isRead).length} уведомлений</small></div><ArrowRight size={16}/></Link></div></section></div>{role==="admin"&&<div className="dashboard-secondary-grid"><section className="crm-panel table-panel"><header><div><span>Команда</span><h3>Эффективность менеджеров</h3></div><Link to="/crm/admin/managers">Подробнее <ArrowRight size={15}/></Link></header><div className="manager-ranking">{managerStats.slice(0,5).map((manager,index)=><div key={manager.id}><b>{index+1}</b><span className="avatar-mini">{manager.name.split(" ").map((part)=>part[0]).join("").slice(0,2)}</span><div><strong>{manager.name}</strong><small>{manager.salesCount} продаж</small></div><span className="manager-bar"><i style={{width:`${Math.min(100,manager.conversion*2)}%`}}/></span><em>{manager.conversion}%</em></div>)}</div></section><section className="crm-panel top-products"><header><div><span>Каталог</span><h3>Самые просматриваемые</h3></div><Link to="/crm/admin/analytics">Аналитика <ArrowRight size={15}/></Link></header>{topProducts.map((product,index)=><div key={product.id}><b>{index+1}</b><img src={product.images[0]?.url||"/logo.jpg"} alt=""/><span><strong>{product.name.ru}</strong><small>{product.views.toLocaleString()} просмотров</small></span><em>{product.stock} шт.</em></div>)}</section></div>}</div>;
}
